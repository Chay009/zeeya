{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.2",
    "@trpc/server": "catalog:",
    "@zeeya/api": "workspace:*",
    "@zeeya/auth": "workspace:*",
    "@zeeya/db": "workspace:*",
    "@zeeya/env": "workspace:*",
    "@zeeya/parser": "workspace:*",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@types/node": "^22.13.14",
    "@zeeya/config": "workspace:*",
    "alchemy": "catalog:",
    "tsdown": "^0.22.3",
    "typescript": "^6",
    "wrangler": "catalog:"
  }
}
